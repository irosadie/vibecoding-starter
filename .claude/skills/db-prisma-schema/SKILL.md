---
name: "db-prisma-schema"
description: "Menulis atau mengubah apps/api/prisma/schema.prisma beserta langkah migrasi yang aman untuk PostgreSQL. Gunakan saat ada perubahan model database, relasi, index, enum, atau kebutuhan sinkronisasi schema dengan domain layer."
---

Source of truth ada di `.agents/skills/db-prisma-schema/SKILL.md`.

Saat skill ini dipakai:
1. Baca `.agents/skills/db-prisma-schema/SKILL.md`.
2. Ikuti workflow dan aturan di file tersebut.
3. Baca file turunan yang direferensikan (`references/context.md`, `templates/checklist.md`) dari folder source of truth.
