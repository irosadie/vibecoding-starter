---
name: "web-slicing"
description: "Convert screenshot/Figma designs into pixel-perfect frontend UI with reusable components. Use when the task focuses on visual and layout implementation."
---

Source of truth lives at `.agents/skills/web-slicing/SKILL.md`.

When this skill is used:
1. Read `.agents/skills/web-slicing/SKILL.md`.
2. Follow the workflow and rules defined in that file.
3. Read the referenced sub-files (`references/context.md`, `templates/checklist.md`) from the source-of-truth folder.
